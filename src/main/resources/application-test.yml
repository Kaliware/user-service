# Dados de conexao com o banco H2
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
jwt:
  secret: teste
  expiration: 86400000
user:
  deletion:
    days: 1
    cron: 1 * * * * ?

